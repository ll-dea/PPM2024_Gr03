<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.PPM2024_Gr03">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC6ApT8BWNLtGZ9AvW9XZ5luxP3kA8Qqy0" />

        <!-- Activities -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PPM2024_Gr03">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LogIn"
            android:exported="false" />
        <activity android:name=".SignUp" />
        <activity android:name=".UserHomePage" />
        <activity android:name=".AdminPage" />
        <activity android:name=".UserMenu" />
        <activity android:name=".UserContact" />
        <activity android:name=".AdminMessages" />
        <activity android:name=".NewItem" />
        <activity android:name=".Cart_Activity" />
        <activity android:name=".TableSelectionActivity" />

        <activity android:name=".ProfileActivity_User" />
        <activity android:name=".ChangePasswordActivity" />

        <activity android:name=".ProfileActivity_Admin" />
        <activity android:name=".ChangePassword_Admin" />


    </application>
</manifest>